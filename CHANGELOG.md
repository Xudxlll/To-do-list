# 更新说明

## v2.1.0 - 2026-06-23

这版是一次围绕“今天干什么”和“写日记”的大规模体验升级：选项管理从静态预设变成可双人共享维护，日记标签和活动选项打通，并补齐了云函数写入、基础同步和多处细节稳定性。

### 今天干什么选项管理升级

- 每个子分类旁新增 `＋` 入口，可以直接添加活动名称和补充说明。
- 所有活动支持编辑名称、编辑说明、删除和跨子分类拖拽排序。
- 新增一键收起/展开，方便在大分类下只看子分类结构。
- 拖拽排序时增加放置提示线，松手位置更清楚。
- 搜索栏文案改为 `搜一搜去干啥`，搜索图标更明显。
- 编辑活动弹窗优化名称输入框高度，placeholder 不再被遮挡。

### 云端共享和基础同步

- 新增 `manageOptions` 云函数，活动的新增、编辑、删除和排序统一走云函数写入。
- `custom_options` 集合可以使用“所有用户可读，仅创建者可读写”，避免前端直写被权限卡住。
- 今天干什么页面进入时、回到前台时和停留期间每 15 秒自动刷新一次活动目录。
- 修复云函数写入 `_id` 导致 `不能更新_id的值` 的问题。
- 更新云开发文档，补充云函数上传、集合权限和跨设备验收步骤。

### 日记和标签联动

- 写日记时自动识别标签后，会与今天干什么的共享活动目录同步。
- 已存在的活动标签会直接复用，不存在的新标签会通过同一套共享选项逻辑创建。
- 日记标签编辑修复输入法反复退出的问题，可以连续输入和删除。
- 写日记新增一键清空草稿，清空正文、地点、照片和已选心情。
- 心情改为可不选，新日记默认不选心情，清空草稿后也恢复为空。

### 稳定性和 Skyline 兼容

- 规避微信开发者工具 Skyline 下增强滚动容器触发的 `RawScrollbar` 崩溃。
- 清理今天干什么页面里 Skyline 不支持的样式写法，减少模拟器警告和白屏风险。
- 强化共享选项保存失败、拖拽失败和云端写入失败后的回滚与缓存保护。
- 新增云函数、选项服务、选项管理 UI、标签同步、清空草稿和心情空态等本地检查脚本。

## v2.0.0 - 2026-06-08

这版是一次大版本升级，在原来的“今天怎么过”互动流程之外，新增了独立的情侣日记系统，并补齐了双人共享、标签沉淀和跨设备锁定体验。

### 新增“我们的日记”

- 首页新增独立入口 `我们的日记`，不遮挡原有照片墙和主操作按钮。
- 新增日记首页，支持查看最近日记、当月记录天数和进入月历。
- 新增写日记页面，支持每天一篇共同日记。
- 支持补写过去日期，禁止写未来日期。
- 支持本地草稿，未保存退出后再次进入同一天可以恢复。

### 日记内容能力

- 日记支持正文、地点、最多 3 张照片和多选心情。
- 写日记顶部日期增加农历显示，2026 年法定节假日会额外标注。
- 保存失败时保留草稿和已上传照片，避免重复上传同一批图片。
- 日记按固定文档 ID 保存，避免 `date` 唯一索引配置错误导致只能保存一条。

### 自动识别标签

- 写日记时会根据正文和地点自动识别标签。
- 标签复用“今天干什么”里的分类和选项。
- 地点单独归入 `今天去哪逛`，正文不再重复识别地点字眼。
- 已优化重复标签和噪声标签，例如避免生成“火锅烤肉”“街了”等无效项。
- 未命中的新标签会写入对应大分类的“其他”分组。

### “今天干什么”选项管理

- 云端新增到“其他”分组的标签支持删除。
- 每个分类下的选项支持手动排序。
- 排序按钮改成更简洁的上下箭头。
- 排序结果保存到本地，下次进入仍保持顺序。

### 月历体验升级

- 新增日记月历页。
- 月历改成常规一行七列布局。
- 周一放在最左，周日放在最右。
- 月份标题、左右切换按钮和日期区域按粉色主题重新美化。
- 修复快速切换月份时旧请求覆盖新月份的问题。
- 限制超出农历数据范围的月份跳转，避免异常日期显示。

### 分享与 Perfect 锁定

- 修复“我也来选”进入今天干什么后返回位置不正确的问题。
- 从对象分享入口进入后，返回会回到带 `看看 TA 选了什么` 的欢迎页。
- `Perfect` 锁定状态新增云端同步。
- 任一设备点击 `Perfect` 后，另一台设备普通打开小程序也会进入“今日已定”页面。
- 新增 `locked_plans` 云集合用于保存当天锁定计划。

### 稳定性和上传前清理

- 增加分享数据结构校验，损坏缓存不会导致结果页崩溃。
- 草稿写入增加防抖和异常保护，减少输入时的卡顿风险。
- 导航栏返回按钮增加兜底，特殊入口不再点了没反应。
- 更新云开发配置文档，明确需要 `diaries`、`custom_options`、`locked_plans` 三个集合。
- 增加多组本地检查脚本，覆盖日期、标签、分类选项、排序、分享数据和锁定计划。

## v1.0.1 - 2026-05-26

这版主要把小程序从“直接选择今天干什么”升级成了一个更完整、更有仪式感的互动流程。

### 新增爱心照片首页

- 新增 `welcome` 首页，启动后先看到照片爱心墙。
- 支持 21 张照片，按爱心形状排列。
- 照片已压缩优化，避免包体积过大和黑图问题。
- 首页底部保留两个入口：
  - `今天不安排，随性过`
  - `来看看今天干啥`

### 新增自由输入页面

- 新增 `free-write` 页面。
- 用户可以自由输入一句今日安排。
- 输入后可分享给对方。
- 对方打开后结果页会显示自由文字卡片。

### 分享流程优化

- 分享后对方不会直接进入结果页。
- 对方先看到爱心照片首页。
- 首页底部显示 `看看 TA 选了什么`。
- 点击后才进入收到选择页面，更有仪式感。

### 结果页调整

- 普通选择结果标题改成 `一二&布布的选择`。
- 自由文字结果仍显示自由安排内容。
- `Perfect` 锁定当天计划、重置逻辑继续保留。

### 分类数据重构

- 原来每个主分类下面是扁平选项。
- 现在改成“主分类 + 子分类标题 + 选项”的二级结构。
- 例如“吃”下面分成：
  - 菜系
  - 火锅锅物
  - 烤肉烧烤
  - 小吃快餐
  - 甜品轻食
  - 在家吃

### 选项内容扩充和精简

- 扩充了吃、喝、玩、逛、看、运动、宅家的选项。
- 精简了“玩 / 看 / 运动”之间重复或相近的项目。
- 演出类归到“看”，运动类归到“运动”，互动体验留在“玩”。

### “逛”分类重点升级

- 按最新搜索结果更新了深圳热门去处。
- 每个地点增加了小字说明：
  - 所在区
  - 大概位置
  - 简单推荐理由
- 商圈商场按顺序整理为：
  - 龙华
  - 福田
  - 南山
  - 宝安
  - 罗湖

### 包体积优化

- 21 张照片从几十 MB 压到约 125KB 实际总大小。
- 当前 `miniprogram` 总体积约 300KB。
- 没有单个资源超过 200KB。
- 解决了开发者工具上传时的图片资源体积警告。
